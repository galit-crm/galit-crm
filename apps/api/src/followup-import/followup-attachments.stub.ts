/**
 * תשתית עתידית: ייבוא קבצים / OLEOBJECTS / מסמכים מקושרים מ-Followup.
 * כרגע אין מימוש — ה-hook נשמר כדי לא לערבב לוגיקה עם ה-pipeline הראשי.
 *
 * מתוכנן: זיהוי טבלאות מסמכים ב-dump, העתקה ל-Document / אחסון קבצים.
 */
export const FOLLOWUP_ATTACHMENTS_IMPORT_PLACEHOLDER = true;
